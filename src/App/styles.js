import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    container: {
        height: "100vh"
    },
    navbarContainer: {
        zIndex: 20
    },
    activePageContainer: {
        overflow: "hidden"
    }
});
