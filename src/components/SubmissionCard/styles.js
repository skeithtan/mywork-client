import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    padded: {
        padding: 16
    },
    attachmentPadding: {
        padding: "16px 16px 0px 16px"
    },
    dialogContainer:{
        width: 300,
    },
    sliderPadding:{
      marginBottom:56
    }
});