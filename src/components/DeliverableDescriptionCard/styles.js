import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    alarmIcon: {
        fontSize: '1rem',
        marginRight: 5
    },
    deadlineText:{
      display:'flex',
      alignItems: 'center',
    }
})