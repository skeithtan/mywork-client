import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    deliverableCard: {
        margin: 20,
    },
    alarm: {
        width: 13,
        height: 13,
        marginRight: 3
    },
    deadlineText:{
      display:'flex',
      alignItems: 'center',
    }
})