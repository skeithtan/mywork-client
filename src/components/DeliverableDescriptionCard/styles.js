import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    deliverableCard: {
        margin: 20,
    },
    alarmIcon: {
        fontSize: '1rem',
        marginRight: 5
    },
    deadlineText:{
      display:'flex',
      alignItems: 'center',
    }
})