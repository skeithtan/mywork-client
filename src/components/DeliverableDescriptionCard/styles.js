import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    deliverableCard: {
        width: 800,
        margin: 20,
    },
    alarm: {
        width: 13,
        height: 13,
        marginRight: 3
    },
    deadlineText:{
      display:'flex',
      alignItems: 'center',
    }
})