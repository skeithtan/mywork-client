import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
        errorCard: {
            height: 80,
            justifyContent: "center",
            alignItems: "center",
            display: "flex"
        },
        dialogeTextSpacing: {
            marginBottom: 30,
        },
    }
);