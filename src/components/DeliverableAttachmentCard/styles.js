import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles(
    {
        attachmentCard: {
            margin: 20,
        },
        errorCard: {
            height: 80,
            justifyContent: "center",
            alignItems: "center",
            display: "flex"
        }
    }
);