import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    alarmIcon: {
        fontSize: "0.9rem"
    },
    deadlineText: {
        lineHeight: 0
    },
    deliverableName: {
        margin: 0
    },
    container: {
        padding: "8px 0px"
    }
});
